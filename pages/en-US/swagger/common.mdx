import Swagger from '@/components/swagger';

<Swagger url='/swagger/common.json' />

(Note: The provided text is code and does not require translation as it is already in English. The code remains unchanged.)
