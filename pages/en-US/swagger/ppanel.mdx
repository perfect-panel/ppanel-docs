import Swagger from '@/components/swagger';

<Swagger url='/swagger/ppanel.json' />

(Note: The provided text is code and does not require translation as it is already in English. The structure and syntax of the code remain unchanged.)
