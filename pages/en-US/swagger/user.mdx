import Swagger from '@/components/swagger';

<Swagger url='/swagger/user.json' />

This code snippet imports the `Swagger` component from the specified path and renders it with a URL pointing to the user JSON file for Swagger documentation.
